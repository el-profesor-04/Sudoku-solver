# Sudoku-solver

The Procedure solve takes a 9x9 array from the user (0s representing empty spaces), and fills in remaining numbers according to the rules of sudoku game.
The method does a brute force search recursively.
